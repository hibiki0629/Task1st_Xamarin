<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="BlankApp1.Views.StackLayoutPage"
             Title="StackLayout">
    <StackLayout>
        <Label Text="{Binding Text}" VerticalOptions="CenterAndExpand" HorizontalOptions="Center"/>
        <Button Text="{Binding Text}" VerticalOptions="CenterAndExpand" Command="{Binding ButtonClick}"/>
    </StackLayout>
  
</ContentPage>